<% if @service.statut %>
  Ce service a déjà été rendu
<% else %>
  Ce service est toujours demandé par <%= @service.user.name %>
<% end %>
<br>
<p>
  <strong>Title:</strong>
  <%= @service.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @service.description %>
</p>

<p>
  <strong>Place:</strong>
  <%= @service.place %>
</p>
<p>
  <strong>Nombre de personnes:</strong>
  <%= @service.nbpart %>
</p>

<p>
  <strong>Transport:</strong>
  <%= @service.transport %>
</p>

<p>
  <strong>Price:</strong>
  <%= @service.price %>
</p>

<p>
  <strong>Date:</strong>
  <%= @service.date %>
</p>


<% unless @service.statut %>
<%= link_to 'Edit', edit_service_path(@service) %> 
|
<%= link_to 'Back', services_path %> | <%= link_to 'Participer à l\'évènement', '/services/'+@service.id.to_s+'/participer' %>
  <% if defined? session[:user_id] %>
    <% if session[:user_id] == @service[:user_id] %>
  | <%= link_to 'Service rendu !', '/services/'+@service.id.to_s+'/terminate' %>
    <% end %>
  <% end %>
<% end %>